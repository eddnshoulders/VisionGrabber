#!/bin/bash
APP_DIR="$HOME/VisionGrabber/backend" 
PYTHON="$APP_DIR/grabber-venv/bin/python" 
SEND_CMD="$APP_DIR/send_cmd.py"

echo "=============================="
echo " VisionGrabber System Restart" 
echo "=============================="
echo

echo "Systemd services:"
printf " %-24s %s\n" "Launcher:" "$(sudo systemctl stop VisionGrabberLauncher)" 
printf " %-24s %s\n" "App:" "$(sudo systemctl stop VisionGrabberApp)"
printf " %-24s %s\n" "Button:" "$(sudo systemctl stop VisionGrabberButton)"


echo 
echo "Machine state:"
cd "$APP_DIR" || exit 1
"$PYTHON" "$SEND_CMD" STATE
